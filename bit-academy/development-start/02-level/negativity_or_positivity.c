#include <stdio.h>
#include <math.h>

int main ()
{
    int target;

    printf ("voer getal in:");

    scanf ("%d", &target);

    if (target > 0)
    
    {

    printf ("hakuna matata");
    } else {
        printf (" gebeurt niks");
    }
    return 0;
}